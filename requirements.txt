# ============================================================
#  AI Powered Virtual Drawing & 3D Modeling Platform
#  requirements.txt  —  Enhanced version with CNN
# ============================================================
#
#  QUICK INSTALL:
#    pip install "numpy>=1.24.0,<2.0" --upgrade --force-reinstall
#    pip install "opencv-python>=4.8.0,<5.0"
#    pip install "mediapipe>=0.10.13,<0.11"
#    pip install -r requirements.txt
#
#  Then train the CNN bootstrap model:
#    python train_gesture_cnn.py
#
#  Then run:
#    python main.py
# ============================================================

# Core vision
numpy>=1.24.0,<2.0
opencv-python>=4.8.0,<5.0
mediapipe>=0.10.13,<0.11

# 3D rendering
PyOpenGL>=3.1.7
PyOpenGL_accelerate>=3.1.7
trimesh>=4.0.0,<5.0
Pillow>=10.0.0

# CNN gesture model (pick ONE — PyTorch preferred)
# Option A: PyTorch (recommended, ~2GB install)
torch>=2.0.0
# Option B: scikit-learn (lightweight fallback, ~50MB)
scikit-learn>=1.3.0

# Shape geometry helpers
scipy>=1.11.0

# Collaborative drawing (optional)
# websockets>=12.0

# Voice commands (optional)
# SpeechRecognition>=3.10.0
# pyaudio>=0.2.13
